<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="tc.dao.InstitutionFieldDAO">
    <select id="selectById" resultType="tc.model.InstitutionField">
        select * from institutionField where id = #{id}
    </select>

    <select id="selectByType" resultType="tc.model.InstitutionField">
        select * from institutionField where type = #{type}
    </select>

    <select id="select" resultType="tc.model.InstitutionField">
        select * from institutionField;
    </select>
</mapper>